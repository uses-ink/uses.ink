= Hello, Typst!

As you can see, `uses.ink` is not only limited to Markdown files! 

Typst is a modern *typesetting language* designed to make the creation of complex documents simpler and more intuitive. Combining the power of LaTeX with a more user-friendly syntax, Typst allows for greater flexibility and ease of use in formatting scientific papers, reports, and technical documents.

$ det(A) = sum_(i=1)^n (-1)^(i+1) a_i det(A_i) $

