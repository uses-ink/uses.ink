```rust showLineNumbers copy {1,2} caption="A simple Hello, world!" title="main.rs"
fn main() {
    println!("Hello, world!");
}
```

This is `const a = "caca"{:js}` and this is `const b = "caca"{:ts}`
